#pragma once

namespace functionParameters {
extern float RE_MIN;
extern float RE_MAX;
extern float IM_MIN;
extern float IM_MAX;
extern float STEP;
}  // namespace functionParameters

namespace settingsWindow {
extern bool SHOW_SETTINGS_WINDOW;
}
