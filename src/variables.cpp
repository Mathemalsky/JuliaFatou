#include "variables.hpp"

namespace functionParameters {
float RE_MIN;
float RE_MAX;
float IM_MIN;
float IM_MAX;
float STEP;
int MAX_ITER;
}  // namespace functionParameters

namespace settingsWindow {
bool SHOW_SETTINGS_WINDOW;
}
